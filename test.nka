import test_lib.nka

fn help {
    
    // welp, this is a comment
    ++;

    clear;

    if is_wall {
        fill;
        set_value(2);
    }

    set_value(5);

    (v) {
        clear;
        if is_flag {
            recall([v]);
        }

        // very bizzare lambda tests
        {
            clear;

            () {
                clear;
            };

            {
                fill;
            }();

            (lv){
                set_value([lv]);
            }(5);
        };
    } (1);

    for 65535 {
        clear; step_for(15);
    }

    recall;

    --;
}